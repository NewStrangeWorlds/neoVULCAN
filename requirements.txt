numpy>=2.2
pyfastchem>=4.0       # equilibrium chemistry initialisation (replaces bundled C++ FastChem)
scipy>=1.15
sympy>=1.14        # used by make_chem_funs.py; will be retired in Phase 2
matplotlib>=3.10
Pillow>=11.0       # optional: for live plot display; code falls back gracefully if absent
jax>=0.6.2
jaxlib>=0.6.2
pydantic>=2.0,<3   # typed config schema (replaces vulcan_cfg.py loose globals)
tomli>=2.0; python_version < "3.11"   # TOML parser; stdlib tomllib used on 3.11+
disortpp           # optional: required only when photochemistry.rt_scheme = "disort"

