# neoVULCAN configuration: schema defaults reference
#
# Generated from neovulcan_config.VulcanConfig by _gen_defaults_toml.py.
# Every field is shown with its default value (used when the TOML omits it).
# Required fields are marked "# REQUIRED" and shown with a placeholder.
#
# This file is for DOCUMENTATION ONLY — copy a section into your own TOML
# and override what you need. To regenerate after a schema change:
#   python _gen_defaults_toml.py

[network]
# REQUIRED:  atom_list = []  # list[str]
# REQUIRED:  network = "..."  # str
gibbs_text = "thermo/gibbs_text.txt"  # str
com_file = "thermo/all_compose.txt"  # str
cross_folder = "thermo/photo_cross/"  # str
use_lowT_limit_rates = false  # bool
remove_list = []  # list[int]

[paths]
fastchem_dir = "fastchem_input"  # str
# REQUIRED:  atm_file = "..."  # str
# REQUIRED:  sflux_file = "..."  # str
top_BC_flux_file = "atm/"  # str
bot_BC_flux_file = "atm/"  # str
vul_ini = "output/"  # str
output_dir = "output/"  # str
plot_dir = "plot/"  # str
movie_dir = "plot/movie/"  # str
# REQUIRED:  out_name = "..."  # str

[elements]
use_solar = false  # bool
metal_abundances = {}  # dict[str, float]
ini_mix = "const_mix"  # 'EQ' | 'const_mix' | 'vulcan_ini' | 'table'
fastchem_met_scale = 1.0  # float
const_mix = {}  # dict[str, float]

[atmosphere]
atm_base = "N2"  # 'H2' | 'N2' | 'O2' | 'CO2'
rocky = true  # bool
# REQUIRED:  nz = 0.0  # int
# REQUIRED:  P_b = 0.0  # float
# REQUIRED:  P_t = 0.0  # float
use_Kzz = true  # bool
use_moldiff = true  # bool
use_vm_mol = false  # bool
use_vz = false  # bool
atm_type = "file"  # 'isothermal' | 'analytical' | 'file' | 'vulcan_ini' | 'table'
Kzz_prof = "file"  # 'const' | 'file' | 'Pfunc'
K_max = 1.0e+05  # float
K_p_lev = 0.1  # float
# (omitted by default) K_deep  # optional float
vz_prof = "const"  # 'const' | 'file'
# REQUIRED:  gs = 0.0  # float
Tiso = 1000.0  # float
para_warm = [120.0, 1500.0, 0.1, 0.02, 1.0, 1.0]  # list[float]
# (omitted by default) para_anaTP  # optional list[float] — aliased to atmosphere.para_warm when omitted
const_Kzz = 1e10  # float
const_vz = 0.0  # float
update_frq = 100  # int
r_star = 1.0  # float
# REQUIRED:  Rp = 0.0  # float
orbit_radius = 1.0  # float
sl_angle_deg = 58.0  # float
f_diurnal = 0.5  # float
# (derived property) sl_angle  # radians = math.radians(sl_angle_deg) — read-only

[photochemistry]
use_photo = false  # bool
use_ion = false  # bool
scat_sp = []  # list[str]
T_cross_sp = []  # list[str]
edd = 0.5  # float
dbin1 = 0.1  # float
dbin2 = 2.0  # float
dbin_12trans = 240.0  # float
ini_update_photo_frq = 100  # int
final_update_photo_frq = 5  # int
rt_scheme = "two-stream"  # 'two-stream' | 'disort'
surface_albedo = 0.0  # float
disort_nstr = 8  # int

[boundary_conditions]
use_topflux = false  # bool
use_botflux = false  # bool
use_fix_sp_bot = {}  # dict[str, float]
diff_esc = ["H"]  # list[str]
max_flux = 1e13  # float
use_sat_surfaceH2O = false  # bool
use_fix_H2He = false  # bool
loss_ex = []  # list[str]

[condensation]
use_condense = false  # bool
use_settling = false  # bool
use_relax = []  # list[str]
humidity = 1.0  # float
r_p = {}  # dict[str, float]
rho_p = {}  # dict[str, float]
start_conden_time = 0.0  # float
stop_conden_time = 5e8  # float
condense_sp = []  # list[str]
non_gas_sp = []  # list[str]
fix_species = []  # list[str]
# (omitted by default) fix_species_time  # optional float — aliased to condensation.stop_conden_time when omitted
fix_species_from_coldtrap_lev = true  # bool
use_ini_cold_trap = true  # bool

[solver]
ode_solver = "Ros2"  # 'Ros2' | 'Rodas3' | 'ODESolver'
use_pi_controller = false  # bool
use_print_prog = true  # bool
use_print_delta = false  # bool
print_prog_num = 500  # int
dttry = 1e-10  # float
trun_min = 100.0  # float
runtime = 1e22  # float
dt_min = 1e-14  # float
# (omitted by default) dt_max  # optional float — derived as solver.runtime * 1e-5 when omitted
dt_var_max = 2.0  # float
dt_var_min = 0.5  # float
count_min = 120  # int
count_max = 40000  # int
atol = 0.1  # float
mtol = 1e-22  # float
mtol_conv = 1e-16  # float
pos_cut = 0.0  # float
nega_cut = -1.0  # float
loss_eps = 1e12  # float
yconv_cri = 0.01  # float
slope_cri = 1.0e-04  # float
yconv_min = 0.1  # float
flux_cri = 0.1  # float
flux_atol = 1.0  # float
st_factor = 0.5  # float
conv_step = 500  # int
conver_ignore = []  # list[str]
rtol = 1.0  # float
post_conden_rtol = 0.05  # float
use_adapt_rtol = true  # bool
rtol_min = 0.02  # float
rtol_max = 2.5  # float
use_newton_finisher = false  # bool
newton_switch_dy = 1.0  # float
newton_cooldown = 200  # int
newton_max_iter = 20  # int
newton_res_tol = 1e-6  # float
newton_alpha_min = 0.001  # float

[output]
output_humanread = false  # bool
use_shark = false  # bool
save_evolution = false  # bool
save_evo_frq = 10  # int
y_time_freq = 1  # int

[plotting]
plot_TP = false  # bool
use_live_plot = false  # bool
use_live_flux = false  # bool
use_plot_end = false  # bool
use_plot_evo = false  # bool
use_save_movie = false  # bool
use_flux_movie = false  # bool
plot_height = false  # bool
use_PIL = true  # bool
live_plot_frq = 10  # int
# (omitted by default) save_movie_rate  # optional int — aliased to plotting.live_plot_frq when omitted
plot_spec = []  # list[str]
